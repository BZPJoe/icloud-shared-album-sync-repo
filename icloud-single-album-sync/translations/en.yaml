{
  "config": {
    "step": {
      "user": {
        "title": "iCloud Shared Album Sync",
        "description": "Configure the sync options for your iCloud Shared Albums.",
        "data": {
          "keep_days": "Keep days (0 = forever)",
          "max_files": "Max files to keep",
          "timeout": "Request timeout (seconds)",
          "interval_minutes": "Sync interval (minutes)",
          "debug": "Enable debug logging",
          "mirror_missing": "Mirror deletions from album",
          "prune_dry_run": "Dry run for pruning (no deletions)",
          "albums": "Albums YAML configuration"
        }
      }
    },
    "error": {
      "invalid_yaml": "Invalid YAML in albums field."
    },
    "abort": {
      "already_configured": "Add-on is already configured."
    }
  },
  "options": {
    "step": {
      "init": {
        "data": {
          "keep_days": "Keep days (0 = forever)",
          "max_files": "Max files to keep",
          "timeout": "Request timeout (seconds)",
          "interval_minutes": "Sync interval (minutes)",
          "debug": "Enable debug logging",
          "mirror_missing": "Mirror deletions from album",
          "prune_dry_run": "Dry run for pruning (no deletions)",
          "albums": "Albums YAML configuration"
        }
      }
    }
  }
}
